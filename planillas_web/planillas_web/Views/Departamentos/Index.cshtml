@model IEnumerable<planillas_web.Models.Departamentos>

@{
    ViewBag.Title = "Index";
}

<h2>Departamentos</h2>

<p>
    @Html.ActionLink("Crear nuevo", "Create")
</p>
<table class="table">
    <tr>
        <th>
           Nombre
        </th>
        <th>
           Descripcion
        </th>
        <th>
           Empresa
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.nombre)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.descripcion)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Empresas.nombre)
        </td>
        <td>
            @Html.ActionLink("Editar", "Edit", new { id=item.ID_departamento }) |
            @Html.ActionLink("Eliminar", "Delete", new { id=item.ID_departamento })
        </td>
    </tr>
}

</table>
